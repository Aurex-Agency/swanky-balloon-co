# Client-visible presentation controls. Set false and rebuild for the live site.
NEXT_PUBLIC_DEMO_MODE=true
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Server only. Without this, /api/inquiry succeeds in demo mode without delivery.
GHL_WEBHOOK_URL=
# Reserved for a future direct email adapter; not used by the current GHL adapter.
OWNER_NOTIFICATION_EMAIL=
RESEND_API_KEY=
